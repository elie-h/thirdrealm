import { Network } from "@prisma/client";

export function checkTokenOwnership(
  wallet_addr: string,
  contract_addr: string,
  network: Network
) {
  // Check cache first

  // Check alchemy response

  // Check if token is owned by wallet

  // Update cache

  // Return result
  return true;
}

// Update Collection model to support the query above!
