table:
  schema: public
  name: users
array_relationships:
  - name: space_memberships
    using:
      foreign_key_constraint_on:
        column: user_wallet
        table:
          schema: public
          name: space_memberships
