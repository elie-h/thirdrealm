table:
  schema: public
  name: wallets
array_relationships:
  - name: space_memberships
    using:
      foreign_key_constraint_on:
        column: wallet_id
        table:
          schema: public
          name: space_memberships
